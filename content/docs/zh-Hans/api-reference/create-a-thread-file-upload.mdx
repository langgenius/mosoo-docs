---
title: 创建 Thread 文件上传
full: true
_openapi:
  preload:
    - zh-Hans
  method: POST
  webhook: false
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          创建作用于此 Thread 的 Files API 上传会话。请求只提供文件元数据；Mosoo 会从 Thread 解析对应的 App 和
          Session，并创建 Session 附件上传。MVP 公开上传必须不超过 67108864 字节，并使用单次 PUT。
---

{/* This file is generated from the Mosoo OpenAPI snapshot. Run npm run openapi:pages after changing the spec. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="zh-Hans" operations={[{"path":"/threads/{threadId}/files/uploads","method":"post"}]} showDescription />
    </>
  );
}